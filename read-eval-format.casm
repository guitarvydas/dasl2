def
  ~.kind = sync
  ~.name = "read eval format"
  ~.input[0]=((buffer))
  ~.output[0]=((atoms) (formatted buffer))
  ~.inputs = [~.input[0]]
  ~.outputs = [~.output[0]]
  ~.ancestor = "top"
  ~.locals = []
  ~.input-queue = []
  ~.output-queue = []
  ~.before = %fn{%asc{$inject ~.(buffer) >> [format](buffer)}}
  ~.handler = %handler{}
  ~.after = %fn{%asc{%return ~.locals.(atoms) ~.locals.(formatted buffer)}}
  ~.children = [
    "reader" : [reader] >> (atoms) (index)
    "eval" : [eval](e) >> (result)
    "format" : [format](index)(buffer) >> (formatted buffer)
  ]
  ~.connections = [
    [reader](index) >> [eval](e)
    [eval](result) >> [format](index)
  ]

event slot
static slot
send