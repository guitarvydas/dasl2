in top.asc
asc [read-eval-format](buffer) >> (atoms (formatted buffer))
    $inject (buffer) >> [format](buffer)
    $wait
    $return { atoms (formatted buffer) }

    components
        [reader] >> (atoms) (index)
	[eval](e) >> (result)
	[format](index)(buffer) >> (formatted buffer)
    end components

    connections
        [reader](index) >> [eval](e)
	[eval](result) >> [format](index)
    end connections
    
end [read-eval-format]