def
  ~.kind = sync
  ~.name = "top"
  ~.input[0]=("NIL" "atom space" "cons space" "src" "out")
  ~.output[0]=("out")
  ~.inputs = [~.input[0]]
  ~.outputs = [~.output[0]]
  ~.ancestor = ???
  ~.locals = []
  ~.input-queue = []
  ~.output-queue = []
  ~.before = fn()
  ~.handler = (lambda (message) ...)
  ~.after = fn(%return ~.locals."out")
  ~.children = [
    "read eval format" : [read eval format](src)(out) >> (out)
  ]
  ~.connections = [
    [read eval format](out) >> ~.(out)
  ]
