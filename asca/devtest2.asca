def [successful]
  name [successful]
  etags «conclude» «found» «answer»
  inputs «conclude»
  outputs «found» «answer»
  nets ⇒₁ ⇒₂ ⇒₃ ⇒₄ ⇒₅ ⇒₆ ⇒₇ ⇒₈
  locals found answer name
  initially
  handler
    [
      | «conclude»
          Send «answer» ?data
          Send «found» Yes
    ]
  finally
  children
  connections

signature [successful]
  name [successful]
  etags «conclude» «found» «answer»
  inputs «conclude»
  outputs «found» «answer»
